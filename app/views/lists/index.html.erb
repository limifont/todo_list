<h1>To-DO LIST</h1>
<% @lists.each do |list| %>
	<h3><%= list.title %></h3>
  <h6><%= link_to 'More info.', list_path(list) %> |
    <%= link_to 'Edit', edit_list_path(list) %> |
    <%= link_to 'Delete', list_path(list), method: :delete %></h6>
<% end %>
